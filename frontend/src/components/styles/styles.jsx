import styledComponent from '@mui/material/Box';

import { styled as StyledComponent } from '@mui/material/styles';

const Box = (props) => {
  return <StyledComponent {...props} />;
};

export default Box;
