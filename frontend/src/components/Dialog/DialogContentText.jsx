import DialogContentTextComponent from '@mui/material/DialogContentText';

const DialogContentText = (props) => {
  return <DialogContentTextComponent {...props} />;
};

export default DialogContentText;
